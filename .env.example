# Astro public analytics key (exposed to client)
PUBLIC_GTAG_ID=G-XXXXXXXXXXX

# Set different values per environment in your own .env files
# Create .env.production with your real ID, and leave .env (dev) empty to disable analytics locally.
